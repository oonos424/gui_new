package affr.project;

/**
 * Compressibility model for a calculation.
 *
 * <p>Persisted in {@code .mode} as the enum name (e.g. {@code "COMPRESSIBLE"}).
 */
public enum ComprsModel {
  COMPRESSIBLE,
  INCOMPRESSIBLE,
}
