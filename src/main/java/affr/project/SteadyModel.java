package affr.project;

/**
 * Steady/unsteady model for a calculation.
 *
 * <p>Persisted in {@code .mode} as the enum name (e.g. {@code "STEADY"}).
 */
public enum SteadyModel {
  STEADY,
  UNSTEADY,
}
