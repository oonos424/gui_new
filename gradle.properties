# Configuration cache disabled until all plugins (Checker Framework, Spotless) are confirmed
# compatible with Gradle 9.0. Re-enable and fix warnings when upgrading those plugins.
org.gradle.configuration-cache=false
